import { BuildOptions } from '@srclaunch/types';
import { BuildFormat } from '@srclaunch/types';
export interface ViteBuildOptions extends Omit<BuildOptions, 'bundle' | 'format' | 'platform' | 'splitting' | 'tool' | 'treeShaking'> {
    bundle?: {
        exclude?: string[];
        optimize?: string[];
    };
    format: BuildFormat.CJS | BuildFormat.ESM | BuildFormat.UMD;
}
export declare function build({ assets, bundle, format, formats, input, library, manifest, minify, output, rootDir, sourcemap, target, webApp, }: ViteBuildOptions): Promise<void>;
//# sourceMappingURL=vite.d.ts.map